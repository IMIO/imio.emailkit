<?xml version="1.0" encoding="utf-8"?>
<metadata>
  <version>1000</version>
  <dependencies>
    <!-- SPEC §8.2: ":default extends :base". Everything the runtime needs is
         in :base; :default adds only the browser layer that activates the
         restyled Plone default mails. -->
    <dependency>profile-imio.emailkit:base</dependency>
  </dependencies>
</metadata>
